# <!-- Powered by BMAD™ Core -->
template:
  id: qa-gate-template-v1
  name: Quality Gate Decision
  version: 1.0
  output:
    format: yaml
    filename: qa.qaLocation/gates/1.3.login-security-enhancement.yml
    title: "Quality Gate: 1.3"

# Required fields (keep these first)
schema: 1
story: "1.3"
story_title: "Login Security Enhancement"
gate: "PASS" # PASS|CONCERNS|FAIL|WAIVED
status_reason: "All security requirements implemented with comprehensive test coverage"
reviewer: "Quinn (Test Architect)"
updated: "2025-12-16T12:00:00Z"

# Always present but only active when WAIVED
waiver: { active: false }

# Issues (if any) - Use fixed severity: low | medium | high
top_issues: []

# Risk summary (from risk-profile task if run)
risk_summary:
  totals: { critical: 0, high: 0, medium: 0, low: 0 }
  recommendations:
    must_fix: []
    monitor: []

# ============ Optional Extended Fields ============
quality_score: 95  # 0-100 (optional scoring)
expires: "2026-01-16T00:00:00Z"  # Optional gate freshness window

evidence:
  tests_reviewed: 10
  risks_identified: 0
  trace:
    ac_covered: [1, 2, 3, 4, 5, 6, 7, 8]  # AC numbers with test coverage
    ac_gaps: []  # AC numbers lacking coverage

nfr_validation:
  security: { status: PASS, notes: "JWT security implemented correctly, login failure rate limiting active" }
  performance: { status: PASS, notes: "Redis caching for token blacklist, efficient token refresh" }
  reliability: { status: PASS, notes: "Comprehensive error handling and logging" }
  maintainability: { status: PASS, notes: "Code well-structured with proper separation of concerns" }

history:
  - at: "2025-12-16T12:00:00Z"
    gate: PASS
    note: "All security requirements implemented with comprehensive test coverage"

recommendations:
  immediate: []  # Must fix before production
  future:  # Can be addressed later
    - action: "Consider adding API rate limiting for additional protection"
      refs: ["AuthController.java:57"]
    - action: "Consider adding two-factor authentication for enhanced security"
      refs: ["AuthController.java"]